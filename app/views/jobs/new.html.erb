<%= render "form", booking: @job %>
<%= form_for @job, html: { multipart: true } do |f| %>

  <%= f.label :images, 'Job Images' %>
  <%= f.file_field :images, multiple: true %>

  <%= f.submit 'Create Job' %>
<% end %>
